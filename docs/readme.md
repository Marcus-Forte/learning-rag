# Rendering the Architecture

From the root of the repository, run:

```bash
docker run -it --rm -p 8080:8080 -v ./docs:/usr/local/structurizr structurizr/lite
```

Navigate to `http://localhost:8080/` to visualize.